---
title: Quickstart
description: Get started with ctxfy-vault API in minutes.
---

## Overview

This guide walks you through creating a space, adding a source, and uploading a file via the ctxfy-vault API.

## Prerequisites

- Base URL of the API (e.g. `https://api.ctxfy.io` or your deployment)
- Firebase ID Token or User API Key

## 1. Create a space

```bash
curl -X POST https://your-api-host/api/v1/spaces \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"name": "My Workspace", "description": "First space"}'
```

Response includes `data.id` — the `spaceId` for subsequent calls.

## 2. Create a collection (optional)

Organize sources in a collection:

```bash
curl -X POST https://your-api-host/api/v1/spaces/{spaceId}/collections \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"name": "Documents", "parentId": null}'
```

Use `parentId` to create nested collections.

## 3. Create a source

Create a source by type. For a URL (URI type):

```bash
curl -X POST https://your-api-host/api/v1/spaces/{spaceId}/sources \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "type": "URI",
    "initial_data": {"url": "https://example.com/article"},
    "instructions": {},
    "name": "Sample Article",
    "collectionId": "YOUR_COLLECTION_ID"
  }'
```

Source types: `URI`, `FILE`, `CONTENT`, `COMPOSITION`. See [Concepts](/concepts#sources) for `initial_data` per type.

## 4. Upload a file (S3 presigned URL)

For file-based sources, get a presigned URL first:

```bash
curl -X POST https://your-api-host/api/v1/upload/s3 \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"filename": "report.pdf", "contentType": "application/pdf", "size": 102400}'
```

Response: `uploadUrl`, `publicUrl`, `fileKey`. Upload the file with `PUT` to `uploadUrl`, then create a source of type `FILE` with the `fileKey` in `initial_data`.

## 5. Check source status

```bash
curl https://your-api-host/api/v1/sources/{sourceId} \
  -H "Authorization: Bearer YOUR_TOKEN"
```

Status lifecycle: `CREATED` → `PENDING` → `PROCESSING` → `COMPLETED` or `FAILED`.

## Next steps

<Columns cols="2">
  <Card title="Core Concepts" href="/concepts" icon="layers" horizontal="false">
    Spaces, collections, sources, artifacts.
  </Card>

  <Card title="Authentication" href="/authentication" icon="lock" horizontal="false">
    Firebase JWT and API Keys.
  </Card>

  <Card title="API Reference" href="/api-reference" icon="code" horizontal="false">
    Full endpoint specification.
  </Card>
</Columns>
